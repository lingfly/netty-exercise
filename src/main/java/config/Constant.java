package config;

import io.netty.handler.logging.LogLevel;

public class Constant {
    public static final int MAX_CONTENT_LENGTH = 1024 * 1024 * 100;
    public static final LogLevel LOG_LEVEL = LogLevel.INFO;
}
